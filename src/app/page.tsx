import { ReceiptuaryApp } from "@/components/receiptuary-app";

export default function Home() {
  // Keep top-level route minimal and delegate all UI/state logic to the feature component.
  return <ReceiptuaryApp />;
}
